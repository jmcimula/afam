---
box: debian
# Build definition
build:
  # The steps that will be executed on build
  steps:
    - arjen/hugo-build:
        version: 0.15
        config: ./config.yaml
        flags: --buildDrafts=true
# A step that deploys hugo build